pipeline{
    agent any
    tools{
     maven 'M2_HOME'
   }
    stages{
        stage('maven commandes'){
            steps{
                sh 'mvn clean compile install package'
            }
        }
    }
}